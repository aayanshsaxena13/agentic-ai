# Import statistics...
import statistics as st

# Create the helpers...
def mean(numbers):
    return st.mean(numbers)

def median(numbers):
    return st.median(numbers)

def mode(numbers):
    return st.mode(numbers)